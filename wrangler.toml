# Cloudflare Pages configuration for the Open Stay Pass frontend.
# Build with `pnpm build:pages`, then deploy `dist/public`.
name = "stay-pass"
compatibility_date = "2026-08-28"
pages_build_output_dir = "dist/public"
